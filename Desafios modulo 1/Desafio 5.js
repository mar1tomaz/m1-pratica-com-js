

function processData(cubos,
    cuggbyos) {
    let S = input[0];
    let N = input[1];
    let temOCaractere = [];
    S = S.split(", ");
    N = N.split(", ");
    //CONFIRMAR SE EXISTE TODOS DE S EM N
    for (let letra of S) {
        if (!N.includes(letra)) {
            console.log("NAO");
            return;
        } else {
            temOCaractere.push(letra);
        }
    }
    temOCaractere = temOCaractere.join("");
    if (temOCaractere === S) {
        console.log("SIM");

    } else {
        console.log("NAO");
    }
}






if (input === (input.toLowerCaseCase(){

} else if (input === (input1.toLowerCase()).join(" ")) {
    input2 = input;
} else {
    input2 = input1.toLowerCase()
    let input3 = input2.split(", ");
    letra.push(input3.find[0]);
    input3.shift();
    input3.push(letra);
    input2 = input3.join(", ");
}
console.log(input1.join(" "));
} 
